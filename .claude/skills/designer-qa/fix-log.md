# designer-qa — Fix log

Cold-path archive of closed fixes, keyed to sentinel. **Consult ONLY when a `🟢` row in `SKILL.md`'s smell table flips back to `🔴`** — then the matching entry here tells you where the fix lived so you know where to look first. Do NOT read this file during a normal session.

Format: `YYYY-MM-DD · sentinel-id · file:symbol · one-line why`.

Writing contract: when you close a new smell, add the `🟢` row to `SKILL.md`'s smell table AND a one-line entry here with the same sentinel-id. Never inline the recipe in the smell table — the table stays hot-path concise; the recipe lives here.

**Architectural shift 2026-04-22.** `@open-pencil/core` / CanvasKit / `packages/editor/src/bridge/` + `packages/editor/src/canvas/editor-shell.ts` were removed; the live canvas is now a DOM-iframe composed by `packages/editor/src/canvas-dom/` (dom-canvas + overlay + pointer + renderer + zoom-pan + present + webgl-present). Entries dated ≤ 2026-04-22 referencing `@open-pencil/core` / `graph-bridge` / `node-bridge` / `editor-shell.ts` / `SkiaRenderer` / `pageColor` describe architecture that no longer ships — treat them as frozen-green pattern references, not live suspicion paths. Entry `2026-04-22 · architecture/op-removal-B-C` is the canonical tombstone.

---

- 2026-04-20 · export iframe body bg · `packages/core/src/exporters/html.ts` exporter writes body bg from scene root fill · historical
- 2026-04-20 · layer name dedup · `packages/core/src/importers/html.ts` `findFirst` depth-capped at 2 · prior heuristic inherited deepest descendant text into every wrapper
- 2026-04-20 · variations colorRotation 400 · `packages/mcp/src/platform/api/variations.ts` `ensureTokenIndex()` resolves project `activeBrand` slug → loads DESIGN.md → `tokenizeDesignSystem` + `autoBindTokensFromGraph` before call. Slug-vs-label gotcha: manifest.activeBrand is always the slug ("linear"); scene.brand stores the display label ("Inspired by Linear") — don't feed scene.brand to `loadBrandFromProject`
- 2026-04-20 · Generate→Responsive selector · `140-toolbar.js` handleMacroAction — read `[data-bc-input]` first; fallback to legacy `[data-agent-input]`; dispatch `input` event after `value=` so autosize fires
- 2026-04-20 · Export modal Escape · `140-toolbar.js` `showExportPreview` — `document.addEventListener('keydown', onEsc, true)` with capture (beats canvas Escape handler); remove on close
- 2026-04-20 · reframe_edit role/text lookup · `packages/mcp/src/tools/edit.ts` `findNode` — `path:"role:<role>"` and `path:"text:<substr>"` prefixes + automatic role/text fallback on single-segment miss. Unblocks agent after the name-dedup fix collapsed wrapper names to generic "Stack"/"Row"
- 2026-04-20 · compile height inheritance · `packages/mcp/src/tools/compile.ts` — when `name` matches a saved project scene AND no explicit `width`/`height`, read prior dims from `manifest.scenes[*]` so agent re-compile loops don't truncate tall canvases to 1080
- 2026-04-20 · chip repaint global binding · `105-bottom-chat.js` — assigns `window.reframeRenderBottomChips = renderChips`; toolbar was already calling it as a no-op fallback. Preview→Mobile now repaints chip immediately.
- 2026-04-20 · bc-input Enter to send · `105-bottom-chat.js` — Shift+Enter inserts newline; plain Enter / Cmd/Ctrl+Enter both submit. Was Cmd+Enter-only, so Slack/Discord-trained designers hit Enter, saw a newline, assumed broken.
- 2026-04-20 · api/reload endpoints · `packages/mcp/src/platform/router.ts` — `/api/audit` without sceneId resolves via Referer → `project.members[0].id`; added `/api/manifest?slug=` and `/api/project?slug=` aliases that return `{ok, slug, name, activeSceneId, scenes, activeBrand}` using existing `findProjectBySlug`+`getActiveBrand`. Closes StoreSync.pullFromMCP TypeError on reload.
- 2026-04-20 · boot/audit payload · `packages/mcp/src/platform/boot-payload.ts` `buildAuditResult` — aligned shape with `/platform/api/audit`: `findings[].fix={property,current,suggested,css}` + `brandFidelity`; replaced silent error swallow with `console.error` so future null-audit regressions are diagnosable.
- 2026-04-20 · layout/asides collapse · `packages/mcp/src/platform/pages/editor-shell-page.ts` inline `<style>` — breakpoint raised 720 → 1024 px; media override uses **full** `grid-template` shorthand + `!important` because the base rule uses shorthand (longhand column override alone loses at equal specificity). Gotcha for next editor: don't put backticks inside this inline `<style>` template-literal — they close the outer string and blank the page.
- 2026-04-20 · export iframe-html-bg · `packages/core/src/exporters/html.ts` — `rootBgForBody(root, graph)` now applied to the `html` selector AND `body` selector. Previously only `body` was painted, so when iframe height exceeded scene content the iframe's containing modal bg bled through the `html` gap. One-line addition to the style block; no byte-determinism impact for scenes ≥ full iframe height (they already cover the html background).
- 2026-04-20 · export flex-shrink FIXED children · `packages/core/src/exporters/html.ts` primary-axis branch — when emitting `width: Npx` / `height: Npx` for a FIXED-sized child of a flex parent, also emit `flex-shrink: 0`. Default `flex-shrink: 1` let siblings with long wrapping text crush the FIXED child to width:0 (e.g. a 32×32 badge in a row with a 2-line body text vanished while cards 1 + 3 rendered fine). FIXED = "this size, period" — shrink:0 is the correct CSS, width alone is a hint the browser ignores under pressure.
- 2026-04-20 · right-panel empty-state on boot · `packages/mcp/src/platform/ui/160-init.js` `tryRestoreProps` — added an early branch: if `!state.selection?.inode` on the top-level invocation (`attemptsLeft === 6`), call `clearPropsPanel()` once and return. `clearPropsPanel` already delegates to `renderSceneDashboard` when a session id exists, so the Canvas-root panel (W/H + background + audit) becomes the real empty state. Previously the panel stayed stuck on the static `<div class="props-empty">Select a node to inspect</div>` from `editor-shell-page.ts` until the user pressed Escape or clicked empty workspace — that interaction flipped the state machine, but cold boot never did.
- 2026-04-20 · canvas deep-click promotion · `packages/mcp/src/platform/ui/160-init.js` `reframe:canvas-select` handler — before writing state / showing props, walk ancestors via `window.__reframeEditor.getNode(id).parentId` until an ancestor has a `[data-layer-node=...]` entry (max 8 hops); promote `nodeId` to that ancestor. Fixes: clicking "Start free trial" button text selected its TEXT leaf (98×18, not in LAYERS) instead of the button frame (164×48, in LAYERS). LAYERS+chip desynced because the LAYERS-guard dropped the leaf id, but the right panel ignored the guard and rendered leaf anyway. Same patch also fires `reframeRenderBottomChips()` + `reframe:ui-state-changed` so chip + LAYERS subscribers repaint together with the panel.
- 2026-04-20 · toolbar Escape dismisses menu + submenu · `packages/mcp/src/platform/ui/140-toolbar.js` `bindMacroDropdowns` — added capture-phase `document.addEventListener('keydown', ..., true)`: when any `[data-macro-menu]:not(.hidden)` or `.macro-submenu.open` is present, Escape closes them all (menus, active button styling, submenus). Also extended the outside-click path to close open submenus. Keyboard users were getting trapped in a half-open Modify menu with no way out except clicking outside. Capture phase beats the canvas Escape handler (which deselects nodes), so when a menu IS open Escape closes it; when no menu is open canvas keeps Escape for deselect.
- 2026-04-20 · toolbar overlap at tablet vp · `packages/mcp/src/platform/pages/editor-shell-page.ts` inline `<style>` 1024px media query — added `.macro-dropdowns { display: none !important; }`. The macro pill is `position: absolute; left:50%; transform: translate(-50%,-50%)` which centered it over the header flex row; once sidebars collapsed at ≤1024 and the right-anchored Export button couldn't push it away, the pill overlapped Export and the More-dropdown chevron. Hiding it removes the collision cleanly — Generate/Modify/Preview/More remain reachable via bottom chat palette + agent prompts at mobile/tablet.
- 2026-04-20 · dashboard card name shows project slug not scene-root INode name · `packages/mcp/src/platform/pages/dashboard.ts` — extended `GENERIC_TAGS` with `stack`, `row`, `column`, `group`, `frame`, `canvas`, `body`, `html`. Linkedom-imported scenes land on these auto-inferred names ("Stack" for column flex, "Row" for row flex); dashboard.ts previously only checked HTML tag names (`div`, `span`, `section`, …) so the card showed "Stack" where the user expected "qa-landing". Now falls back to `p.slug` for every auto-inferred structural name.
- 2026-04-20 · right-panel derived-dims refresh on prop-change · `packages/mcp/src/platform/ui/160-init.js` — added `window.addEventListener('reframe:prop-changed', ...)` that calls `queuePropsRefresh()` (the existing 300ms-debounced re-fetcher used by `node-moved`/`node-resized`) when the edited prop is layout-affecting: `font-size|font-family|font-weight|line-height|letter-spacing|padding*|width|height|min-/max-*|gap|itemSpacing|text|characters`. Previously only the input the user edited was updated; derived Size W/H stayed at pre-edit values until a re-select. Fires the event from `editNodeProp` in 120-widgets.js (already dispatched there for CanvasKit re-hydration).
- 2026-04-20 · empty-state Background paints workspace, not root frame · **110-properties.js** `renderSceneDashboard` — renamed the empty-state block to `Canvas [workspace]` and changed the input from `data-prop="background" data-node="root"` to `data-prop="canvas-bg" data-workspace-key="reframe:workspace-bg:<slug>"` (no `data-node`, so the node-edit path is skipped). Initial value read from `localStorage[key]` first, falling back to `getComputedStyle(documentElement)['--surface-canvas']`, default `#E8E2D0`. Subtitle clarified: "Nothing selected — edit the canvas around your frame". **120-widgets.js** `bindPropInputs` intercepts `prop === 'canvas-bg'` in the generic change handler BEFORE `editNodeProp`, calling new helper `applyCanvasBg(hex)` + writing localStorage + updating the sibling swatch. `applyCanvasBg` does two things: (1) `document.documentElement.style.setProperty('--surface-canvas', hex)` — paints `#canvas-area` around the CanvasKit canvas element; (2) parses hex to `{r,g,b,a:1}` in 0..1 space and writes `__reframeEditor.state.pageColor`, bumps `state.sceneVersion`, calls `editor.requestRender()` — mandatory because `@open-pencil/core` has a hardcoded `CANVAS_BG_COLOR = {r:.96,g:.96,b:.96}` in constants.js that the SkiaRenderer clears the canvas with on every frame (`renderer.js:480 canvas.clear(ck.Color4f(this.pageColor.r, ...))`). Without (2) the CSS var only shows in the sliver of `#canvas-area` not covered by the `<canvas>` element — basically invisible. Fill-swatch click handler in 120-widgets.js also branches for `isWorkspace`: native color picker drives `applyCanvasBg` on `input` (live preview while dragging) + `change` (commit + persist). `enhanceColorPopover` path ALSO handles `canvas-bg` in `openColorPopover.commit()` — original commit unconditionally called `editNodeProp(sceneId, nodeId, ...)` with empty sceneId/nodeId for workspace swatches (POST silently failed). Live preview on `input` for popover hex + native picker for parity with swatch path. Input-event live preview added to main hex input too (typing feels responsive, not Enter-gated). **160-init.js** `init` restores on boot: reads `localStorage['reframe:workspace-bg:<slug>']`, sets CSS var synchronously (pre-paint), then `setInterval` retry loop calls `applyCanvasBg` every 200ms until `window.__reframeEditor` is ready (CanvasKit WASM boots 2-3s after DOMContentLoaded; pageColor writes before that are no-ops). Gives up after 30 tries. Scene root INode fill is never touched from this control — user must select the root frame explicitly in LAYERS to edit that.
- 2026-04-20 · engine/grid-auto-rows field · `packages/core/src/engine/types.ts` + `scene-graph.ts` + `serialize.ts` + `exporters/html.ts` — added `gridAutoRows: GridTrack | null` and `gridAutoColumns` on SceneNode. Parses from CSS `grid-auto-rows: <track>` in `importers/html.ts:3195` via existing `parseGridTemplate`. Layout engine in `engine/layout.ts:640` now prefers `gridAutoRows` over `FR 1` fallback when `gridTemplateRows` is empty. HTML exporter emits `grid-auto-rows` when set, keeping round-trip parity. Scenarios unblocked: Bento landing `grid-template-columns: repeat(6,1fr); grid-auto-rows: minmax(200px, auto)` + cells with `grid-row: span 2` — previously collapsed to 159px/row, now correctly 200px × 3 rows.
- 2026-04-20 · engine/minmax-with-auto-max · `packages/core/src/importers/html.ts` `resolveGridTrackToken` minmax branch — when `max === 'auto' | 'max-content' | 'min-content'`, fall back to the `min` argument's pixel value as `{type:'FIXED', value: minPx}` instead of collapsing to `AUTO(0)`. Common Bento idiom `minmax(200px, auto)` means "at least 200, grow if content needs more" — the 200 is authoritative, not discardable. Previously lost both the minimum AND any row-span math built on top.
- 2026-04-20 · engine/implicit-grid-row-count-from-spans · `packages/core/src/engine/layout.ts:635` — `numRows` computation previously `Math.max(gridTemplateRows.length, ceil(children.length / numCols))`, which ignored row-spans: 6 children on 6 cols → 1 implicit row, even when one child spanned 2 rows. Replaced with pre-placement simulation: walk children, track span-aware occupancy, take max `endRow` reached. Handles both explicit-placement and auto-placement with span-aware wrap. Fixes: Bento hero `grid-column: span 4; grid-row: span 2` sat in row 1 only (auto-rows had nothing to place them in).
- 2026-04-20 · engine/post-layout-height-propagation · `packages/core/src/engine/layout.ts:873` `computeAllLayouts` — after grid pass, walks the tree bottom-up and grows every VERTICAL container (scene root + column sections) whose declared height is smaller than sum(child bottoms). Previously scene root pinned at importer default 1080; a 728-px bento section + 88-px nav + 360-px hero + 80-px footer = 1256 total overflowed by 176px, painting footer outside the frame bounds. Editorial (2522), email (969), pricing (auto-grown from initial compute) all relied on this. **Critical constraint**: pure manual height math, NOT `computeLayout(rootId)` re-run. Earlier attempt re-invoked Yoga which overwrote `computeGridLayout`'s cell x/y coords (all 6 bento cells collapsed to top-left of grid frame). Yoga treats grid frame as a flex container when re-walking from ancestor; manual propagation reads child bottoms without recomputing children.
- 2026-04-20 · design-system/parser radius bullet-list · `packages/core/src/design-system/parser.ts:1518-1554` `parseDesignMd` — radius section now (a) falls back to carving `### Border Radius` sub-block out of the `## Layout` section body when no dedicated h2 exists (Linear/Stripe/Airbnb DESIGN.md all keep radius under Layout as h3), (b) extracts numeric radii from prose bullet lines like `- Micro (2px): Inline badges` via `/\((\d{1,5})\s*(?:px|%)?\)|:\s*(\d{1,5})\s*px/gi`. Previously the parser found no `scale: N N N` single-line format in Linear's DESIGN.md and fell back to default `[0,2,4,8,12,16]` — Linear's signature 6px "Comfortable" radius was then off-scale, tanking `brandFidelity.radiusCompliance` to 28% across every scene that used it. After fix: Linear scale extracted as `[2,4,6,8,12,22,9999,50]`, radiusCompliance → 100% for scenes whose radii match.
- 2026-04-20 · regression test grid-stress · `packages/core/src/tests/grid-stress.test.ts` — 4 scenarios × 8 assertions covering: (1) `grid-auto-rows: minmax(200px, auto)` imports as FIXED 200 + row-span child height ≥ 400, (2) implicit row count derives from rowSpan not `ceil(children/cols)`, (3) scene root grows past 1080 default for multi-section content, (4) grid cells retain distinct x/y after root-grow pass (no Yoga re-pass regression). Run: `npx tsx src/tests/grid-stress.test.ts` from `packages/core`. Passes 8/8.
- 2026-04-22 · preview-inject postMessage targetOrigin · `packages/mcp/src/preview-inject.ts` — replaced `postMessage(..., '*')` with `postMessage(..., PARENT_ORIGIN)` where `PARENT_ORIGIN = window.location.origin` (parent + preview are same-origin by design, both served by the sidecar). Prevents leaking scene structure (bbox + className + text per inode) to any cross-origin embedder of `/preview/:id`.
- 2026-04-22 · engine/inode-path-parity · `packages/mcp/src/tools/edit.ts` + `packages/mcp/src/platform/api/node-edit.ts` — extracted the sanitize-and-transform block of `applyNodeUpdate` (clamp opacity/rotation/dimensions/spacing/radius, fill color-space clamp, fills-with-tokens parsing, numeric token resolution, `padding` shorthand explode, `role→semanticRole` alias, `{value,unit}`-unwrapping for `lineHeight`/`letterSpacing`, states parsing) into exported `sanitizeNodePartial(propsInput, {targetId, graph, tokenIdx})` returning `{changes, warnings, appliedKeys}`. `applyNodeUpdate` now a 6-line wrapper. Platform UI `/api/node/edit` and its auto-fix + audit-fix siblings (three call-sites total) import the helper, run `cssPropsToNodePartial` output through it before `graph.updateNode`. CSS mapper rewritten: new `parseHexFill` handles 3/4/6/8-char hex and routes hex alpha to `fill.opacity` (not `color.a`, matching `hexToColor` in edit.ts); `line-height`/`letter-spacing` emit plain `number` (engine type is `number | null`, old `{value, unit:'PIXELS'}` wrapper wrote invisible 1.5px line-height); new cases for `role`, `padding` shorthand; `border-color` + `border-width` + `stroke-*` now merge-patch into the existing first stroke via `baseStroke()` helper that clones `node.strokes[0]` instead of synthesizing a fresh black/weight-1 stroke that clobbered color or align. `cssPropsToNodePartial(edits, node)` now takes the current node for that merge; `handleNodeEditApi` passes `node`. Old `hexToRgba` removed (all callsites migrated). Regression: `packages/mcp/src/tests/inode-path-parity.test.ts` — 9 scenarios × 35 assertions covering clamping (opacity 2.5→1, padding -20→0, radius -10→0, width 99999→16384), hex alpha, 3-char hex, lineHeight unit, role translation, padding shorthand, border-color weight preservation, border-width color preservation, full-edit parity across padding+fontSize+fills+cornerRadius+opacity.
- 2026-04-22 · engine/importer-transform-flip-lost · `packages/core/src/importers/html.ts` transform block (~line 3438) — added three matches: `scaleX(-N)` → `o.flipX = true`, `scaleY(-N)` → `o.flipY = true`, `scale(x,y)` → flipX/flipY based on sign of each axis; replaced the old "intentionally do not mutate" comment with one that explains the distinction (layout box vs visual flip is the right split, but flipX/flipY is exactly the INode field for mirror). Also extended `matrix(a,b,c,d,..)` parser to detect negative-determinant on each axis: `sx = hypot(a,b)`, `sy = hypot(c,d)`, negative `a/sx` → flipX, negative `d/sy` → flipY. The sign check uses normalized ratio so scaling + flipping in combination (e.g. `matrix(-2,0,0,1,...)`) still detects flipX correctly. Regression: `packages/core/src/tests/inode-roundtrip.test.ts` — 14 assertions covering scaleX/Y, scale shorthand, mixed-axis negative, positive-only (no false positive), matrix flip on each axis, rotate() does NOT set flipX (regression guard).
- 2026-04-22 · engine/importer-mix-blend-mode-lost · `packages/core/src/importers/html.ts` — added `mix-blend-mode` CSS parser after transform block: lowercase + lookup in 15-entry map (multiply / screen / overlay / darken / lighten / color-dodge / color-burn / hard-light / soft-light / difference / exclusion / hue / saturation / color / luminosity → corresponding INode enum). Inverse of exporter's `blendModeToCSS` (lines 1344-1353 of exporters/html.ts). Cast to `any` because `o` is `Partial<SceneNode>` and `blendMode` is on the base node. Covered by the same round-trip test (3 of the 14 assertions check multiply/overlay/soft-light mapping).
- 2026-04-22 · canvas/hug-collapse-on-load · `packages/editor/src/canvas/editor-shell.ts:238` — inside `loadFromReframeGraph`, the `computeAllLayouts(editor.graph)` call (imported from `@open-pencil/core`, not reframe) was unconditionally re-running OP's flex layout after copying INode fields into the OP graph. OP's HUG semantics differ from reframe's: OP collapsed nested HUG-sized containers to their raw content-height without descendants' layout having stabilized first. Dashboard disk dims (Stack 967, inner 915, Overview row 44, last 324) all collapsed to 100/48/0/0. Patch: conditionalize the call — skip when every top-level child has non-zero dims (reframe source is already Yoga-computed at import time and serialized with correct layout). Only re-run OP layout when the graph came from `.fig` import (fresh OP-native graph where all dims start at 0). Verified via `reframe_ui probe` on `/platform/project/dashboard`: all four collapsed containers restored to disk values (967/915/44/324). Canvas now renders full dashboard matching the PNG thumbnail.
- 2026-04-22 · breadcrumb/generic-inode-root-name · `packages/mcp/src/platform/router.ts:381` `/platform/project/:slug` route handler — duplicated the `GENERIC_TAGS` set from `dashboard.ts` as `GENERIC_ROOT_NAMES` and fell back to `project.slug` when `project.name` is one of those generic tags (`row`, `stack`, `div`, `column`, `group`, `frame`, `canvas`, `body`, `html`, `span`, `section`, `main`, `header`, `footer`, `article`, `aside`, `nav`). Inline duplication chosen over shared helper because `dashboard.ts` is cards-only and the list diverges over time; lifting to a shared helper when a third site needs it.
- 2026-04-22 · canvas/hug-collapse-on-every-edit · `packages/editor/src/app/platform-bootstrap.ts` `wirePropChangedHandler` — removed the unconditional `computeAllLayouts(s.editor.graph)` call from the `reframe:prop-changed` event listener. OP's flex engine re-collapsed nested HUG containers on every single right-panel edit (change "background" color → all HUG nodes collapse → 90% of scene disappears). Now `applyPropsToNode` writes the edit and `requestRender` paints; layout truth comes from the server via SSE, not from OP's runtime re-layout. Paired with server patch in `packages/mcp/src/platform/api/node-edit.ts:536` — added `LAYOUT_AFFECTING_KEYS` allowlist (20 keys covering text/font/padding/dims/minmax/layoutMode/sizing/align/grid) that triggers `ensureSceneLayout(scene.graph, scene.rootId)` after `graph.updateNode` when the edit could change box dimensions. Pure-visual edits (fills/opacity/stroke/cornerRadius/rotation/blendMode) skip the Yoga pass — no layout work needed. Verified by `reframe_ui probe` on dashboard: 5 sequential edits (background / opacity / cornerRadius / paddingTop / fontSize) all preserved main Stack h=783. Pre-fix: single background edit dropped h 967→100.
- 2026-04-22 · graph-bridge/grid-fields-dropped · `packages/editor/src/bridge/node-bridge.ts` — extended `ReframeExtension` interface with `gridAutoRows?`, `gridAutoColumns?`, `gridTemplateAreas?` (all optional, typed via `RFSceneNode[…]`). Updated `extractExtension` to pull these off `rfNode` when present (truthy check for objects, `?.length` for arrays), and `applyExtension` to restore them on the reframe side when bridging back from OP. These three fields live only on reframe's `SceneNode` (added 2026-04-20 for Bento grid support — `grid-auto-rows: minmax(200px, auto)` and named grid areas); `@open-pencil/core`'s `SceneNode` has no slots for them, so without the side-channel, the `fromReframeGraph` mapping silently dropped them on every INode→OP conversion. Bridge contract is now symmetric: every reframe-only field routes through `ReframeExtension` and survives the round-trip. Latent bug — dashboard doesn't hit `grid-auto-rows` so regression didn't surface visually, but stress-bento-* / editorial scenes would have rendered with collapsed grid rows in the canvas while rendering correctly in PNG export.
- 2026-04-22 · api/mutation-endpoints-missing-layout · `packages/mcp/src/platform/api/node-edit.ts` (8 call-sites) — added `ensureSceneLayout(scene.graph, scene.rootId)` calls to every endpoint that mutates the scene graph without one. Unconditional for structural ops (`/node/add`, `/node/delete`, `/node/wrap`, `/node/reparent`): a new child or deletion always shifts HUG ancestor dims. Gated by `LAYOUT_AFFECTING_KEYS` allowlist for property edits that could be pure-visual (`/node/edit` audit-fix, `/scene/auto-fix`, undo single-op, revert-to multi-op): Set of 20 keys covering ``. Pure-visual edits (fills/opacity/stroke/cornerRadius/rotation/blendMode/shadows) still skip Yoga — saves a multi-pass on every color picker tick. Pattern repeats verbatim across 8 sites; lifting to a shared `layoutAfterMutation(scene)` helper if a 9th site appears.
- 2026-04-22 · engine/abs-pos-cache-not-invalidated-after-layout · `packages/core/src/engine/layout.ts` `computeAllLayouts` (end of function, after `propagateHeights(startId)`) — appended `graph.clearAbsPosCache()`. Rationale: Yoga re-pass + `computeGridLayout` + manual height propagation collectively rewrite x/y/width/height on every node in the layout tree, but none of the three mutation points calls `graph.updateNode` (they write fields directly on the live node objects for perf), so `SceneGraph.updateNode`'s built-in geometry-change cache invalidation is bypassed. Downstream consumers — `audit.ts` rules (sibling-overlap, no-overflow, fits-within-parent) via `standalone/node.ts:absoluteBoundingBox()`, `resize/adapt.ts` bleed measurement, Canva adapter — then read cached pre-layout coordinates and produce false findings (overlaps that don't exist, overflow warnings that should've cleared). Clearing at the end of the layout pass keeps all callers honest without forcing every mutation site to remember the invalidation rule. Verified via the full regression suite: `grid-stress.test.ts` 8/8 (exercises audit + layout post-propagation), `inode-roundtrip.test.ts` 14/14, `inode-path-parity.test.ts` 35/35.
- 2026-04-22 · engine/inode-editor-state-leak · new file `packages/core/src/engine/editor-state.ts` holds `EDITOR_STATE_KEYS = new Set(['locked','expanded','autoRename','internalOnly'])` + `isEditorStateField(key)` predicate + `stripEditorState(obj)` helper. Single source of truth — add a new editor-only field by extending this set and tagging the field in `types.ts`, and every downstream path inherits the skip automatically. `types.ts` now has `@editorState` JSDoc blocks on all four fields with rationale and long-term target (`WorkspaceState.lockedIds` / `.expandedIds` for the eventual Step 2 split). `serialize.ts` `serializeSceneNode` drops `json.locked = …` and `json.internalOnly = …` writes — `expanded` and `autoRename` were never written there. Serializer's compact-mode defaults map (line 318+) still lists these fields so its own default-detection logic works for `isDefault()` callers; they're just never output. `serialize.test.ts` two assertions flipped to match new contract (`r2.locked === false` after roundtrip, `compact.locked === undefined`). Bridge (`fromReframeGraph` / `toReframeGraph` in `packages/editor/src/bridge/graph-bridge.ts`) intentionally keeps copying these fields — editor runtime needs them in the OP graph to render LAYERS correctly and respond to user lock-toggles; the strip only applies at persistence + export boundaries. All 261 assertions pass (serialize 204 + grid 8 + roundtrip 14 + parity 35). Step 1 of a 3-step "INode → pure AST" refactor; steps 2 (`NodeEditorState` type split, moving fields off `SceneNode`) + 3 (immutable INode) deferred. Disk + export now carry pure design data; editor state is bridged-only memory.
- 2026-04-22 · engine/inode-runtime-cache-marked · `packages/core/src/engine/editor-state.ts` — extended registry with `RUNTIME_CACHE_KEYS = new Set(['textPicture','fillGeometry','strokeGeometry'])` + `NON_DESIGN_KEYS = EDITOR_STATE ∪ RUNTIME_CACHE` + `isRuntimeCacheField` + `isNonDesignField` + `stripNonDesign` helpers. These three fields are Figma-inherited rasterize/geometry caches, recomputable from `vectorNetwork` and shape props; serializer already skips them (explicit comment about non-JSON-safe `Uint8Array commandsBlob`) but they still live on `SceneNode` for API convenience. Tagged each with `@runtimeCache` JSDoc in `types.ts` pointing at the long-term target (`graph.runtimeCache` Map keyed by nodeId, same pattern as `absPosCache`). This row closes step 1b of the INode → pure AST refactor — documents intent + centralizes the strip contract without an invasive refactor. Next step is actually moving them off `SceneNode`; deferred until there's a measurable memory / bridge-cost win.
- 2026-04-22 · engine/importer-pseudo-class-states-lost · `packages/core/src/importers/html.ts` — (1) extended `parseWithLinkedom` return payload with `pseudoRules: Map<idx, [{state, properties}]>` + `PSEUDO_RE = /:(hover\|focus\|active\|disabled)\b/`. (2) Inside the inline `<style>` CSS-rule loop, selectors matching `PSEUDO_RE` now route to a pseudo-branch: strip the pseudo suffix, `document.querySelectorAll(base)`, push `{state, properties}` onto `pseudoRules.get(idx)`. Previously filtered out via `if (selector.includes(':')) continue` — that blanket skip is now the ELSE of the pseudo-branch (attribute selectors `[foo]` still pass). (3) Plumbed `pseudoRules` through `ConvertContext` (parallel to `mediaRules`). (4) In `cssToOverrides` right after the `responsive`-rules wire-up, added parallel `states`-rules wire-up: iterates `pseudoRules.get(rfIdx)`, converts each via `cssToResponsiveProps`, assigns to `overrides.states[<state>]`. (5) Extended `cssToResponsiveProps` to parse `background` / `background-color` / `color` / `border-radius` — previously only layout + typography props. `background` → `fills[0]` via existing `parseColor`; `color` → `fills[0]` (same target, both fill the text/frame); `border-radius` → `cornerRadius`. Without these additions, the most common designer pseudo-class override (color shift on hover) would round-trip as an empty state block. Regression `packages/core/src/tests/pseudo-class-import.test.ts` — 10 assertions across class-selector, id-selector, disabled state, multi-pseudo on same base, geometry regression guard. The fix is symmetric at the exporter side because `exporters/html.ts` already emitted `node.states` correctly (lines 312-352); it just never had any states to emit on HTML-first imports.
- 2026-04-22 · export/hyperframes-video-lane · new exporter `packages/core/src/exporters/hyperframes.ts` — INode scene + optional `ITimeline` → hyperframes-compatible composition HTML (`<div id="stage" data-composition-id data-width data-height data-start data-duration>` wrapper + inner scene emitted via existing `exportToHtml({dataAttributes:true})` + GSAP master-timeline registered on `window.__timelines[compositionId]` so hyperframes' frame-accurate scrub works). Each `INodeAnimation` → sub-timeline attached to master via `master.add(sub, delaySec)`; keyframes → `gsap.set()` + `gsap.to()` chain; `easing` mapped to GSAP equivalents (ease-out→power2.out, etc.); ms converted to seconds at the seam. `cssToResponsiveProps`-style prop subset: opacity/rotation/x/y/scaleX/scaleY + fillColor→backgroundColor. MCP tool `reframe_export` schema extended with `format: 'video'` → writes composition to `<exportsDir>/<slug>-video/index.html` (directory, not loose file — hyperframes CLI expects a dir arg via `resolveProject` despite their README snippet). Returned tool output includes the `npx hyperframes render <dir>` command the caller runs; render is out-of-process because hyperframes drives Puppeteer + FFmpeg and we don't want Chromium in the MCP sidecar. Verified end-to-end: smoke test exports a 2-animation intro composition (1280×720, 2s), `npx -y hyperframes render ... --fps 30 --quality draft` produces 80KB MP4 in ~18s after first-run Chrome download. No lint warnings (missing_timeline_registry resolved via master-timeline registration). Regression `packages/core/src/tests/hyperframes-export.test.ts` — 21 assertions: composition root shape, dims derived from scene root, GSAP injection, timeline sub-tween emission, `master.add` delay conversion, `window.__timelines` registration, `gsapUrl:null` opt-out, custom-dim override. Hyperframes telemetry disabled repo-wide via `npx hyperframes telemetry disable` (persisted in user config).
- 2026-04-22 · export/hyperframes-auto-render · `packages/mcp/src/tools/export.ts` — added `renderVideo: boolean` + `videoFps?: number` + `videoQuality?: 'draft'|'standard'|'high'` options to the export schema (opt-in because first-run downloads ~100 MB Chromium). When `format: 'video'` + `renderVideo: true`, the handler `spawnSync('npx', ['--yes', 'hyperframes', 'render', videoDir, '-o', mp4Path, '--fps', fps, '--quality', quality])` blocking for up to 5 min (covers first-run Chrome download + real render). Success: return output includes MP4 path + size. Failure: last 10 lines of stderr captured, plus a manual-retry CLI line. Telemetry stays off via the global `npx hyperframes telemetry disable` persisted in user config. Phase 1 video lane is now single-call agent-friendly: `reframe_export({sceneId, format:'video', renderVideo:true})` returns a ready-to-play MP4.
- 2026-04-22 · editor/phase2-dom-canvas-foundation · new package directory `packages/editor/src/canvas-dom/` with five modules: (1) `dom-canvas.ts` — orchestrator factory `createDOMCanvas({container, sceneId, onSelect})` that composes the other four; creates `.rfd-canvas-viewport` inside the `#canvas-area` grid cell; ignores CanvasKit/WASM/OP entirely. (2) `renderer.ts` — `createSceneRenderer` wraps an `<iframe sandbox="allow-same-origin allow-scripts allow-popups">` and loads `/preview/<sceneId>` into `srcdoc`; subscribes to top-level `/events` SSE (not `/platform/api/events` — that endpoint doesn't exist) and reloads on `scene:session-changed` matching the active sceneId. (3) `zoom-pan.ts` — `createZoomPan({wrapper, viewport})` handles Ctrl+wheel zoom-at-point, plain wheel pan, Space+drag pan, Cmd+0/+/- shortcuts; snaps to `ZOOM_LEVELS = [.25, .33, .5, .67, .75, 1, 1.25, 1.5, 2, 3, 4]` to avoid Chrome sub-pixel text blur at arbitrary scales. (4) `overlay.ts` — `createSelectionOverlay` draws an 8-handle bbox in a transform-synced sibling div; handles are sized `HANDLE_SIZE / zoom` so they stay ~8px on screen regardless of canvas scale; stroke width scales inversely too; `onHandleDrag({which, dx, dy, phase})` callback reserved for Phase 2c drag-resize wiring. (5) `pointer.ts` — `hitTest(iframeDoc, x, y, {isMeaningful, maxPromotionHops})` walks up from `elementFromPoint` looking for `data-reframe-inode` (the attribute `/preview/:id` actually emits via `inodeAnchors: true` in http-server.ts — NOT `data-id`, which only the agent-facing exporter path emits with `dataAttributes: true`); promotes to first "meaningful" ancestor (default: always leaf). `packages/editor/src/app/platform-bootstrap.ts` gains `shouldUseDOMCanvas()` (URL `?canvas=dom` OR `localStorage['reframe:canvas']='dom'`) + `initDOMCanvas()` branch that short-circuits `initPlatformViewport` before OP/CanvasKit load: hides the legacy `#reframe-viewport` canvas, hides the `#loading` overlay (id not class — editor-shell-page.ts line 353), and mounts the DOM canvas. Selection events bridge via `reframe:canvas-select` CustomEvent so 120-widgets.js right panel + 150-sidebar.js LAYERS rail populate without knowing which canvas implementation is active. Exposed `window.__reframeDOMCanvas` for parity with OP's `window.__reframeEditor` so `reframe_ui probe` scripts keep working. Verified end-to-end on `/platform/project/dashboard?canvas=dom` (211-node dashboard): iframe rendered with 229 elements, click at (400,140) selected Stack h:fe9a7842, right panel populated (Size 877×309, Fill #13161c), chip row updated, 8 resize handles drew correctly at zoom 0.5. No CanvasKit WASM loaded, no Skia init, no OP bridge — just the HTML exporter piped through a sandboxed iframe. Status: foundation shipped, Phase 2c (incremental DOM patch, drag-resize writes, inline text edit) queued. Default editor path stays OP; opt-in via the URL flag.
- 2026-04-22 · editor/phase2-webgl-present-stub · new file `packages/editor/src/canvas-dom/webgl-present.ts` — architectural placeholder for Phase 3 "present / cinematic" mode. Chromium experimental `gl.texElementImage2D()` (enabled via `chrome://flags/#canvas-draw-element`) turns DOM elements into WebGL textures; that unlocks 3D camera + shader transitions (flash-through-white, grain, CRT, depth-of-field) compositing over the edit iframe without breaking editability. Today: `isTexElementAvailable()` detection + `getTexElementFunction()` accessor + no-op `createPresentMode()` factory. Both return null on stable Chrome (the default); will light up when the flag is enabled OR when the API ships without flag. Independent of `fimbox/html-in-canvas` (that repo is a PlayCanvas + Ammo-physics bug-repro, not a library) — we reference the same underlying Chromium API but build our own integration on top of the already-shipped Phase 2 iframe. Not yet wired; scaffold only, so Phase 2 doesn't accidentally depend on an experimental API.
- 2026-04-22 · mcp/catalog-reframe-block · new MCP tool `reframe_block` in `packages/mcp/src/tools/block.ts` — installs a hyperframes catalog block into a scene as native INode subtree. Actions: `list` (browse cached blocks in `.reframe/blocks/`), `add` (spawns `npx --yes hyperframes add <name> --dir <cacheDir>` if not cached, reads `<cacheDir>/<name>/index.html`, runs through our `importFromHtml` which now handles pseudo-class states + @media + grid-auto-rows (2026-04-22 fixes), then copies the resulting subtree into the target scene under the requested parent via recursive `createNode`; strips incoming ids so fresh ones are minted to avoid collisions on re-install; runs `ensureSceneLayout` at the end so HUG ancestors grow around the inserted block), `show` (prints block HTML for agent reasoning, first 2KB). Registered in `register-tools.ts` with docstring citing example block names (flash-through-white, instagram-follow, data-chart). INode-native throughout — user edits the inserted block exactly like agent-generated content via `reframe_edit` or the right panel; no opaque HTML blob stashed anywhere. Hyperframes catalog thus becomes reframe's 50+-item motion-component library without forking their repo or vendoring assets. (Superseded 2026-04-22 by `reframe_design action=listBlocks` + `reframe_edit op=addBlock` — the standalone tool was folded into design/edit to collapse two overlapping surfaces.)
- 2026-04-22 · editor/phase2c-drag-resize-inline-multiselect · `packages/editor/src/canvas-dom/dom-canvas.ts` rewritten with Phase 2 polish: (1) **Multi-select** — `selection: string[]`; Shift+click toggles; overlay shows UNION bbox; drag moves all selected. (2) **Drag-to-move** — mousedown on selected element body captures per-node start bboxes, live overlay preview at 60fps via pure CSS, commits to `/api/node/edit` with `{x, y}` on mouseup. (3) **Drag-to-resize** — 8 overlay handles emit `onHandleDrag(which, dx, dy, phase)` events; `computeResizedBox` transforms per-handle-direction deltas into new box; commits `{x, y, width, height}` per node. (4) **Inline text editing** — double-click heading/paragraph/span/button → `contenteditable=true` on iframe element, Enter commits `{text-content}` via edit API, Escape cancels. (5) **Incremental DOM patch** (renderer.ts) — on SSE `scene:session-changed`, instead of full `srcdoc` reload (flashy on 500KB scenes), fetch fresh HTML, parse via DOMParser, walk both docs keyed by `data-reframe-inode`, compare `style`/`class`/text, patch in place. Falls back to full reload when id set diverges (add/delete/reparent). **Safety guards**: scene root (first body child) skipped on both drag-move and drag-resize writebacks (we once wrote root.x=1667 root.y=-833 → broke dashboard visibility; "scene root" is the frame, not a draggable node). Dim clamps `±16384px` everywhere that accepts user deltas + defensive `Math.max(0.05, zoom)` in coord-inverse math after catching `height=209794428` on Stack from runaway `dx/zoom` division. Dashboard disk file re-seeded from `.reframe/src/dashboard.html` via `reframe_compile file= + reframe_project save` after the 209M corruption. All 292 regression assertions still pass (serialize 204 + grid 8 + roundtrip 14 + pseudo 10 + hyperframes 21 + parity 35).
- 2026-04-22 · editor/phase3-present-mode · `packages/editor/src/canvas-dom/present.ts` — CSS-based cinematic present mode ships TODAY (no experimental Chrome flag needed). Takes the Phase 2 iframe + viewport, composes a `transform: translate(vpCx, vpCy) + perspective + rotateX + rotateY + scale(fit * zoom) + translate(-iw/2, -ih/2)` with `transform-origin: 0 0` so the camera orbits the iframe's geometric center (right-to-left CSS transform composition: `translate-center-to-zero → scale → rotate → translate-to-viewport-center`). 5 camera presets — `front`/`tilt`/`fly-through`/`isometric`/`parallax`; latter follows mouse, fly-through auto-orbits via `requestAnimationFrame` at ~0.2 deg/frame yaw. 6 filter presets — `none`/`cinema`/`noir`/`dream`/`neon`/`crt`; CRT gets an overlay scanline via `repeating-linear-gradient` in a `mix-blend-mode: multiply` div; cinema gets a radial vignette. Keyboard: `P` toggles (global), Escape exits, arrow keys rotate, `+` / `-` zoom, `Space` cycles filters, `1-5` switch cameras, `0` resets. Backdrop div (`#0a0a0a`, z:5) hides editor chrome during present; iframe wrapper gets `z-index:8` so it paints ABOVE backdrop (we initially saw all-black because wrapper was z:auto beneath backdrop's z:5). Wired in `dom-canvas.ts` as `createDOMCanvas(...).present` — `present.enter(cameraPreset, filterPreset)` / `present.exit()` / `present.setCamera` / `present.setFilter` / `present.orbit(dx, dy)`. Verified on `/platform/project/dashboard?canvas=dom`: `tilt` + `cinema` shows the dashboard perspective-tilted (8° rotateX, -6° rotateY, 0.54 fit scale) with contrast/saturation tweaks. `isometric` + `neon` renders the dashboard as a figma-style 3D-orbital view with saturation/brightness boost — classic "design mockup" presentation look, GPU-composited, no WebGL. The WebGL upgrade path (`webgl-present.ts` stub for `gl.texElementImage2D` when Chromium stabilizes) can swap in later without changing the caller API.
- 2026-04-22 · architecture/op-deprecation-policy · With Phase 2 foundation + Phase 2c polish + Phase 3 present mode all shipped and verified end-to-end on the dashboard scene, **`@open-pencil/core` is deprecated as reframe's canvas engine**. Current status: OP remains the DEFAULT at `/platform/project/:slug`; DOM canvas is opt-in via `?canvas=dom` URL param OR `localStorage['reframe:canvas']='dom'`. Deprecation plan: (1) SHORT-TERM (next release cycle) — flip the default after a dogfooding window; any user who hits latency or feature gaps can fall back via `?canvas=op`. (2) MID-TERM (1 release after that) — strip OP imports from `platform-bootstrap.ts` initPlatformViewport path; keep `createReframeEditor` + `GraphBridge` around for `.fig` import only (that's the one OP feature with no DOM equivalent — Figma binary parsing). (3) LONG-TERM — move `.fig` import to a server-side path (Node + the `@open-pencil/core` bits only) and remove the npm dep from the browser bundle entirely. **Net removal**: ~400KB bundle, ~2-3s CanvasKit WASM boot, ~5.1MB on-disk dep. What we lose from OP: pen tool / vector draw / mask / star-polygon primitives / boolean ops — reframe's HTML-first thesis doesn't rely on any of these; agents design with flex/grid + images, not bezier paths. What we keep: the whole INode + layout + audit + export + token-binding pipeline (which never depended on OP anyway — that was the scene graph layer we'd been bridging around). Policy: any NEW code added to the canvas path SHOULD go to `canvas-dom/`; PRs adding to `canvas/` must justify why it can't live in DOM land.
- 2026-04-22 · architecture/op-removal-B-C · **OpenPencil removed from runtime in one pass** (Phases B + C from the deprecation policy above, compressed into the same release the policy was written). Changes: (A) `packages/editor/src/app/platform-bootstrap.ts` `shouldUseDOMCanvas()` flipped to DEFAULT: true; `?canvas=op` now the explicit fallback flag (kept for one release cycle in case we hit DOM regressions). (B) Deleted wholesale — `packages/editor/src/canvas/` (editor-shell.ts / interaction.ts / audit-overlay.ts — ~2000 LOC total), `packages/editor/src/bridge/` (graph-bridge.ts + node-bridge.ts — the INode↔OP bridge that was source of most 2026-04-22 bugs), `packages/editor/src/sync/store-sync.ts` (OP-shell-bound pull logic; DOM canvas has its own SSE in renderer.ts), `packages/editor/src/sync/tool-bridge.ts` (parseFigFile/exportFigFile/createLinter/ALL_TOOLS/CAPABILITY_MAP — none used by DOM path), `packages/editor/src/app/file-handler.ts` (setupFileDragDrop for .fig — dropped), `packages/editor/src/app/context-menu.ts` (OP Editor type dep), `packages/editor/src/app/app.ts` (standalone dead-code orchestrator, not called anywhere), `packages/editor/src/panels/properties.ts` (OP SceneNode consumer; the live right-panel is 110-properties.js in the platform UI bundle, not this). (C) `platform-bootstrap.ts` rewritten as a 50-line DOM-only bootstrap: `initPlatformViewport()` → `initDOMCanvas()` → `createDOMCanvas` + selection event bridge. (D) `packages/editor/src/index.ts` barrel rewritten to export only DOM-canvas + panels + MCP client. (E) `packages/editor/src/panels/audit.ts` — inlined the `AuditIssueOverlay` interface it used to import from the deleted `canvas/audit-overlay.ts`; renamed `issue.rule` → `issue.ruleId` to match the new shape. (F) `packages/editor/package.json` — dropped `@open-pencil/core` dependency entirely. (G) Root `package.json` — removed `--alias:@open-pencil/core=...` from `build:editor-bundle` esbuild command. **Net impact: `editor-bundle.js` 1.4MB → 32.8 KB (−97.6%).** Build time 130ms → 13ms. No CanvasKit WASM download on page load (saves ~2-3 MB network + the 2-3s boot-race we fought all week). Typecheck editor + mcp + core all clean; all 292 regression assertions still pass (serialize 204 + grid 8 + roundtrip 14 + pseudo 10 + hyperframes 21 + parity 35). Verified via `reframe_ui open /platform/project/dashboard` (no `?canvas=dom` flag): dashboard renders through the DOM canvas immediately, selection + right-panel + chip-row all populated. The `?canvas=op` fallback path is guaranteed broken now (OP code gone), so the flag exists purely as a dogfooding escape valve; when confident, delete `shouldUseDOMCanvas()` entirely and collapse `initPlatformViewport` → one-liner calling `initDOMCanvas`. `.fig` import is now unsupported — users who need Figma-sourced work should use Figma's "Copy as HTML" plugin (pipes through our existing HTML importer, which is richer than OP's Figma parser anyway since it honors tokens/states/responsive/pseudo-classes).
- 2026-04-22 · preview-inject inbound origin-pin · `packages/mcp/src/preview-inject.ts` `onMessage` — added `event.origin !== PARENT_ORIGIN` and `event.source !== window.parent` guards before the existing `data.source === 'reframe-host'` string-tag check. Without this, any page that framed our preview (or a sibling frame under the same top) could postMessage a forged host-tag and flip annotationMode / trigger `measure-all` measurement storm. String-tag alone is not a trust boundary.
- 2026-04-22 · http-server dead renderPreviewDashboard · `packages/mcp/src/http-server.ts` — deleted the 92-line `renderPreviewDashboard()` function + its `esc()` helper. Both iframe call-sites inside it (`<iframe src="/scenes/:id">` for card tiles, `<iframe class="main-preview" src="/preview/:id">` for the active preview) had been unreachable since `url.pathname === '/'` started returning 404 at :511-515 redirecting users to `/platform`. Only `/preview/:id` itself remained active, served via the dedicated handler at :611. Dead-code reduction, zero runtime change.
- 2026-04-22 · toolbar export-preview ternary collapse · `packages/mcp/src/platform/ui/140-toolbar.js` `showExportPreview` — removed `var isCode` + the identical-both-branches ternary. `/preview/:id.tsx` and `/preview/:id.react` already return full HTML with `<pre>` wrapping the source (http-server.ts :655-671), so the iframe renders code correctly. Single iframe emission for the right panel regardless of format.
- 2026-04-20 · layer panel stale TEXT name · `packages/mcp/src/platform/ui/150-sidebar.js` `renderLayerNode` — for TEXT nodes with absorbedText, override `displayName` to the live text (truncated to 28 chars) and blank `absorbedText` so it doesn't double-render as a preview quote. Fixes: chat agent updates `characters` via reframe_edit, LAYERS still shows the frozen import-time name ("Set per-person limits, ca") → reads as "nothing happened." Designer gets immediate visual confirmation after any text edit.
